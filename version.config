# 적토마 레이스 게임 버전 설정
# 이 파일을 수정하여 배포 버전을 구분할 수 있습니다.

# 배경색 (CSS gradient)
BACKGROUND_COLOR="linear-gradient(135deg, #667eea 0%, #764ba2 100%)"

# 버전 표시 문구
VERSION_TEXT="적토마 레이스 v1.0"

# 테마 색상
PRIMARY_COLOR="#ff6b6b"
SECONDARY_COLOR="#4caf50"

# 배포 환경 표시
ENVIRONMENT="PRODUCTION"

# 사용법:
# 1. 이 파일의 값들을 수정
# 2. build.sh 실행하여 새 WAR 파일 생성
# 3. deploy.sh 실행하여 배포
