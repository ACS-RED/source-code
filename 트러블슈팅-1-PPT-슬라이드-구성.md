# 트러블슈팅 #1: Leader Election - PPT 슬라이드 구성

## 📊 슬라이드 구성 (총 7장, 발표 시간 3분 30초)

---

## 슬라이드 0: 게임 구조 소개 (30초) ⭐ 추가

### 제목
```
프로젝트 소개: Polling 기반 실시간 경마 게임
```

### 내용
```
┌─────────────────────────────────────────────┐
│ 적토마 실시간 레이스 (Jeoktoma Race)        │
├─────────────────────────────────────────────┤
│ 게임 방식:                                  │
│ ├─ 4마리 말의 경주                          │
│ ├─ 사용자 베팅 (승리 시 2배 지급)           │
│ └─ 30초 대기 → 10초 경주 → 10초 결과        │
│                                             │
│ 실시간 동기화: Polling 방식                 │
│ ├─ 클라이언트: 1초마다 GET /api/status      │
│ ├─ 서버: DB에서 현재 상태 조회 후 응답      │
│ └─ WebSocket 대신 선택 (ALB 호환, 구현 단순)│
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 게임 로직의 두 가지 역할:                   │
├─────────────────────────────────────────────┤
│ 1️⃣ 게임 진행 (Write):                       │
│    ├─ 1초마다 타이머 감소, 말 이동          │
│    ├─ 사용자 수와 무관 (연산량 일정)        │
│    └─ 담당: Leader 1대만 ⭐                 │
│                                             │
│ 2️⃣ 사용자 요청 처리 (Read/Traffic):         │
│    ├─ Polling 요청 응답 (GET /api/status)   │
│    ├─ 사용자 수에 비례 (기하급수적 증가)    │
│    └─ 담당: 모든 서버 (ALB 분산)            │
└─────────────────────────────────────────────┘
```

### 시각 자료
```
[Polling 방식 동작]
┌─────────────┐
│ 클라이언트  │
│ (브라우저)  │
└──────┬──────┘
       │ GET /api/status (1초마다)
       ↓
┌─────────────┐
│     ALB     │
└──────┬──────┘
       │ 부하 분산
       ↓
┌─────────────┐  ┌─────────────┐
│   WAS 1     │  │   WAS 2     │
│  (Leader)   │  │ (Follower)  │
│             │  │             │
│ 게임 로직   │  │ 게임 로직   │
│ 실행 ✅     │  │ 대기 ⏸️     │
│             │  │             │
│ 사용자 요청 │  │ 사용자 요청 │
│ 50% 처리    │  │ 50% 처리    │
└──────┬──────┘  └──────┬──────┘
       │                │
       └────────┬────────┘
                ↓
           ┌─────────┐
           │   DB    │
           │ (MySQL) │
           └─────────┘
```

---

## 슬라이드 1: 문제 상황 (30초)

### 제목
```
트러블슈팅 #1: 분산 환경에서의 타이머 동시성 문제
```

### 내용
```
┌─────────────────────────────────────────────┐
│ 환경: AWS EC2 기반 3-Tier 아키텍처          │
│ 목적: WAS 이중화를 통한 고가용성 확보       │
├─────────────────────────────────────────────┤
│ 증상:                                       │
│                                             │
│ WAS 1대 운영:                               │
│   타이머: 30 → 29 → 28 → ... ✅            │
│                                             │
│ WAS 2대 운영:                               │
│   타이머: 30 → 28 → 26 → ... ❌ (2배속!)   │
│                                             │
│ WAS 3대 운영:                               │
│   타이머: 30 → 27 → 24 → ... ❌ (3배속!)   │
│                                             │
│ 결과: DB 데이터 정합성 붕괴                 │
└─────────────────────────────────────────────┘
```

### 시각 자료
```
[다이어그램]
┌─────────┐  ┌─────────┐
│ WAS 1   │  │ WAS 2   │
│ timer-- │  │ timer-- │  ← 동시 실행!
└────┬────┘  └────┬────┘
     │            │
     └─────┬──────┘
           ↓
      ┌─────────┐
      │   DB    │
      │ timer=28│  ← 2초 감소!
      └─────────┘
```

---

## 슬라이드 2: 원인 분석 (30초)

### 제목
```
원인: Spring Scheduler의 구조적 한계
```

### 내용
```
┌─────────────────────────────────────────────┐
│ @Scheduled 어노테이션의 특성:               │
│                                             │
│ ❌ 애플리케이션 메모리 기반                 │
│ ❌ 서버 단위(Local)로 동작                  │
│ ❌ 서버 간 통신 없음                        │
│                                             │
│ 결과:                                       │
│ WAS 인스턴스 수 증가                        │
│   → 스케줄러도 인스턴스 수만큼 증가         │
│   → 동일 작업이 N번 실행                    │
├─────────────────────────────────────────────┤
│ 문제의 본질:                                │
│                                             │
│ 분산 환경에서는                             │
│ "오직 하나의 서버만 실행해야 하는 작업" 존재│
│                                             │
│ 필요한 것:                                  │
│ → Distributed Lock / Leader Election       │
└─────────────────────────────────────────────┘
```

### 코드 예시
```java
// 문제 코드
@Scheduled(fixedRate = 1000)
public void updateTimer() {
    // 모든 WAS에서 동시 실행!
    jdbcTemplate.update(
        "UPDATE race_status SET timer = timer - 1"
    );
}
```

---

## 슬라이드 3: 해결 방안 비교 (30초)

### 제목
```
해결 방안 검토 및 선택
```

### 내용
```
┌──────────────┬──────────┬──────────┬──────────┬────┐
│ 방안         │ 장점     │ 단점     │ 비용     │선택│
├──────────────┼──────────┼──────────┼──────────┼────┤
│ 단일 WAS만   │ 구현 단순│ SPOF 발생│ $0       │ ❌ │
│ 실행         │          │          │          │    │
├──────────────┼──────────┼──────────┼──────────┼────┤
│ Redis        │ 빠른 성능│ Redis    │ $50/월   │ ❌ │
│ Distributed  │ 정석     │ 인프라   │          │    │
│ Lock         │          │ 필요     │          │    │
├──────────────┼──────────┼──────────┼──────────┼────┤
│ Zookeeper    │ 높은     │ 오버     │ $100/월  │ ❌ │
│              │ 신뢰성   │ 엔지니어링│          │    │
├──────────────┼──────────┼──────────┼──────────┼────┤
│ DB 기반      │ 추가     │ DB 소량  │ $0       │ ✅ │
│ 리더 선출    │ 인프라 無│ 부하     │          │    │
└──────────────┴──────────┴──────────┴──────────┴────┘

선택 이유:
✅ 기존 MySQL DB만 활용 (추가 비용 $0)
✅ 구현 복잡도 낮음
✅ 5초 이내 자동 Failover
✅ 프로젝트 규모에 적합
```

---

## 슬라이드 4: 해결 구현 (60초) ⭐ 핵심

### 제목
```
DB 기반 리더 선출 (Leader Election) 구현
```

### 내용
```
┌─────────────────────────────────────────────┐
│ 1단계: DB 스키마 설계                       │
├─────────────────────────────────────────────┤
│ race_status 테이블에 컬럼 추가:             │
│ ├─ leader_ip: 현재 리더 서버 ID            │
│ └─ last_heartbeat: 마지막 생존 시간        │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 2단계: 리더 선출 로직 (Heartbeat)           │
├─────────────────────────────────────────────┤
│ 1초마다 모든 WAS가 실행:                    │
│                                             │
│ 1️⃣ 리더 선출 시도 (원자적 UPDATE)          │
│    UPDATE race_status                       │
│    SET leader_ip = '내 ID'                  │
│    WHERE leader_ip IS NULL OR               │
│          last_heartbeat > 5초 OR            │
│          leader_ip = '내 ID'                │
│                                             │
│ 2️⃣ 성공 여부 확인                           │
│    updatedRows = 1 → 리더 ✅               │
│    updatedRows = 0 → Follower ❌           │
│                                             │
│ 3️⃣ 리더만 게임 로직 실행                    │
│    if (리더) { 타이머 감소, 말 이동 }       │
│    else { 아무것도 안 함 }                  │
└─────────────────────────────────────────────┘
```

### 코드 (핵심만)
```java
@Scheduled(fixedRate = 1000)
public void updateGlobalTimer() {
    // 1️⃣ 리더 선출 시도
    int updatedRows = jdbcTemplate.update(
        "UPDATE race_status SET leader_ip = ?, " +
        "last_heartbeat = NOW() WHERE id = 1 AND (" +
        "leader_ip IS NULL OR " +
        "last_heartbeat < DATE_SUB(NOW(), INTERVAL 5 SECOND) OR " +
        "leader_ip = ?)", serverId, serverId
    );
    
    // 2️⃣ 리더가 아니면 종료
    if (updatedRows == 0) return;
    
    // 3️⃣ 리더만 실행
    jdbcTemplate.update("UPDATE race_status SET timer = timer - 1");
}
```

---

## 슬라이드 5: 동작 원리 & Failover (30초)

### 제목
```
동작 원리 및 자동 Failover
```

### 내용
```
┌─────────────────────────────────────────────┐
│ 정상 상황:                                  │
├─────────────────────────────────────────────┤
│ 시간 0초:                                   │
│   WAS 1: UPDATE 시도 → 성공 → 리더 ✅      │
│   WAS 2: UPDATE 시도 → 실패 → Follower     │
│                                             │
│ 시간 1초:                                   │
│   WAS 1: 타이머 감소 (leader_ip = WAS 1)   │
│   WAS 2: 아무것도 안 함                     │
│                                             │
│ 결과: 타이머 1초만 감소 ✅                  │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 장애 상황 (Failover):                       │
├─────────────────────────────────────────────┤
│ 시간 0초: WAS 1이 리더                      │
│ 시간 5초: WAS 1 다운 ❌                     │
│ 시간 10초:                                  │
│   WAS 2: UPDATE 시도 → 성공 (5초 초과)     │
│   WAS 2: 새 리더로 승격 ✅                  │
│   WAS 2: 즉시 게임 로직 이어서 실행         │
│                                             │
│ Failover 시간: 5초 이내                     │
│ 서비스 중단: 0초 (사용자는 모름)            │
└─────────────────────────────────────────────┘
```

### 시각 자료
```
[정상 상황]
┌─────────┐  ┌─────────┐
│ WAS 1   │  │ WAS 2   │
│ Leader  │  │Follower │
│ timer-- │  │  (대기) │
└────┬────┘  └─────────┘
     │
     ↓
┌─────────┐
│   DB    │
│ timer=29│  ← 1초만 감소 ✅
└─────────┘

[장애 상황]
┌─────────┐  ┌─────────┐
│ WAS 1   │  │ WAS 2   │
│  ❌     │  │ Leader  │
│ (다운)  │  │ timer-- │
└─────────┘  └────┬────┘
                  │
                  ↓
             ┌─────────┐
             │   DB    │
             │ timer=29│  ← 정상 작동 ✅
             └─────────┘
```

---

## 슬라이드 6: 결과 & 학습 포인트 (30초)

### 제목
```
프로젝트 성과 및 학습 포인트
```

### 내용
```
┌─────────────────────────────────────────────┐
│ 정량적 성과:                                │
├─────────────────────────────────────────────┤
│ ✅ 테스트 기간: 72시간 연속 가동            │
│ ✅ WAS 인스턴스: 2~4대 (Auto Scaling)       │
│ ✅ 타이머 오작동: 0건                       │
│ ✅ Failover 시간: 평균 3초                  │
│ ✅ 서비스 중단: 0초                         │
│ ✅ 추가 인프라 비용: $0                     │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Before vs After:                            │
├─────────────────────────────────────────────┤
│ Before:                                     │
│   WAS 2대: 타이머 2배속 ❌                  │
│   WAS 3대: 타이머 3배속 ❌                  │
│   데이터 정합성: 붕괴 ❌                    │
│                                             │
│ After:                                      │
│   WAS 2대: 타이머 정상 ✅                   │
│   WAS 10대: 타이머 정상 ✅                  │
│   데이터 정합성: 완벽 유지 ✅               │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 학습 포인트:                                │
├─────────────────────────────────────────────┤
│ 1. 분산 시스템의 동시성 제어                │
│    → DB Row Lock을 활용한 분산 락           │
│                                             │
│ 2. Heartbeat 기반 장애 감지                 │
│    → 5초 타임아웃으로 안정성 확보           │
│                                             │
│ 3. 적정 기술 (Appropriate Technology)       │
│    → 프로젝트 규모에 맞는 기술 선택         │
│                                             │
│ 4. 비용 효율성                              │
│    → Redis/Zookeeper 없이 문제 해결         │
└─────────────────────────────────────────────┘
```

---

## 🎤 발표 스크립트 (3분 30초)

### 슬라이드 0 (30초) ⭐ 추가
"트러블슈팅을 설명하기 전에, 먼저 저희 프로젝트를 간단히 소개하겠습니다.

저희는 **실시간 경마 베팅 게임**을 개발했습니다.
4마리의 말이 경주하고, 사용자들이 베팅하는 방식입니다.

실시간 동기화를 위해 **Polling 방식**을 사용했는데,
클라이언트가 **1초마다** 서버에 게임 상태를 요청하고
서버는 DB에서 조회한 데이터를 응답합니다.

WebSocket 대신 Polling을 선택한 이유는
**구현이 단순**하고 **ALB와 완벽히 호환**되기 때문입니다.

게임 로직은 두 가지로 나뉩니다.
첫째, **게임 진행 로직**은 타이머 감소와 말 이동으로
사용자 수와 무관하게 항상 동일한 연산량을 가집니다.
둘째, **사용자 요청 처리**는 Polling으로 들어오는
조회 요청을 처리하는 것으로 사용자 수에 비례합니다.

이제 이 구조에서 발생한 문제를 설명하겠습니다."

### 슬라이드 1 (30초)
"앞서 설명한 게임 구조에서, WAS를 2대로 늘리자마자 문제가 발생했습니다.

게임 타이머가 **2배 속도**로 감소하는 현상이 발생했습니다. 
3대로 늘리면 **3배 속도**로 감소했고, 이로 인해 DB 데이터 정합성이 완전히 붕괴되었습니다.

사용자들은 Polling으로 1초마다 상태를 확인하는데,
타이머가 비정상적으로 빠르게 줄어드는 것을 보게 되었습니다."

### 슬라이드 2 (30초)
"원인은 Spring의 @Scheduled 어노테이션이 **서버 단위로 동작**하기 때문입니다.

각 WAS가 독립적으로 스케줄러를 실행하면서, 동일한 타이머 감소 로직이 
서버 수만큼 중복 실행되었습니다.

분산 환경에서는 **단 하나의 서버만 실행해야 하는 작업**을 제어하기 위한 
Distributed Lock 또는 Leader Election 메커니즘이 필요했습니다."

### 슬라이드 3 (30초)
"해결 방안을 검토한 결과, **DB 기반 리더 선출** 방식을 선택했습니다.

Redis나 Zookeeper도 고려했지만, 추가 인프라 비용과 관리 복잡도를 고려하여
기존 MySQL DB만 활용하는 방식을 채택했습니다.

이 방식은 **추가 비용 0원**으로 5초 이내 자동 Failover를 구현할 수 있었습니다."

### 슬라이드 4 (60초) ⭐
"구현 방법은 다음과 같습니다.

먼저 DB 테이블에 **leader_ip**와 **last_heartbeat** 컬럼을 추가했습니다.

그리고 1초마다 모든 WAS가 리더 선출을 시도하는데, 
MySQL의 **UPDATE 쿼리**를 통해 원자적으로 수행됩니다.

조건은 세 가지입니다.
첫째, 리더가 없거나
둘째, 마지막 heartbeat가 5초를 초과했거나
셋째, 내가 이미 리더인 경우입니다.

이 UPDATE가 성공하면 리더가 되어 게임 로직을 실행하고,
실패하면 Follower로서 아무것도 하지 않습니다.

핵심은 MySQL의 **Row Lock**이 동시성을 보장한다는 점입니다.
두 서버가 동시에 UPDATE를 시도해도, DB 내부 Lock에 의해 
**단 하나의 서버만 성공**하게 됩니다."

### 슬라이드 5 (30초)
"정상 상황에서는 WAS 1이 리더로 선출되어 타이머를 감소시키고,
WAS 2는 Follower로서 대기합니다.

만약 WAS 1이 다운되면, 5초 후 WAS 2가 자동으로 리더로 승격되어
게임 로직을 이어서 실행합니다.

이를 통해 **서비스 중단 없이** 자동 Failover가 가능했습니다."

### 슬라이드 6 (30초)
"결과적으로 **72시간 연속 가동 테스트**에서 타이머 오작동 **0건**을 달성했고,
WAS 수와 무관하게 데이터 정합성을 완벽히 유지했습니다.

이 과정에서 분산 시스템의 동시성 제어와 DB를 활용한 분산 락 구현,
그리고 프로젝트 규모에 맞는 적정 기술 선택의 중요성을 배웠습니다."

---

## 💡 발표 팁

### 강조할 포인트
1. **숫자로 말하기**: "2배속", "72시간", "0건", "3초"
2. **Before/After 명확히**: "2배속 → 정상 속도"
3. **비용 강조**: "추가 비용 $0"
4. **기술 깊이**: "MySQL Row Lock", "원자적 UPDATE"

### 시간 배분
- **게임 구조 소개: 30초** (맥락 제공) ⭐ 추가
- 문제 상황: 30초 (임팩트 있게)
- 원인 분석: 30초 (간결하게)
- 해결 방안: 30초 (비교 빠르게)
- **구현 상세: 60초** (가장 중요, 자세히)
- 동작 원리: 30초 (시각 자료 활용)
- 결과: 30초 (성과 강조)

### 주의사항
- 코드는 핵심만 보여주기
- 전문 용어는 설명과 함께
- 청중 눈 맞추며 발표
- 자신감 있게!

---

이제 완벽한 PPT 자료가 준비되었습니다! 🎉
