# 프로젝트 게임 구조 설명 (Polling 기반 실시간 게임)

## 🎮 프로젝트 개요

### 게임 컨셉
```
적토마 실시간 레이스 (Jeoktoma Race)
├─ 장르: 실시간 경마 베팅 게임
├─ 플레이어: 다중 사용자 동시 접속
├─ 게임 방식: 4마리 말의 경주 + 베팅
└─ 특징: 하드코어 룰 (파산 시 계정 삭제)
```

### 게임 흐름
```
1. 대기 단계 (30초)
   ├─ 사용자 베팅 가능
   ├─ 타이머 카운트다운
   └─ 베팅 마감

2. 경주 단계 (10초)
   ├─ 4마리 말이 동시 출발
   ├─ 랜덤 속도로 이동
   └─ 먼저 결승선 도착한 말 승리

3. 결과 단계 (10초)
   ├─ 승리한 말 발표
   ├─ 베팅 성공자에게 2배 지급
   ├─ 파산자 계정 삭제
   └─ 다음 경주 준비

4. 반복 (무한 루프)
```

---

## 🏗️ 게임 아키텍처: Polling 방식

### 왜 Polling 방식인가?

```
실시간 게임 구현 방법 비교:

1️⃣ WebSocket (양방향 통신):
   장점: 진짜 실시간, 서버 푸시 가능
   단점: 구현 복잡, ALB 설정 복잡, Sticky Session 필요
   
2️⃣ Server-Sent Events (SSE):
   장점: 서버 → 클라이언트 단방향 푸시
   단점: 브라우저 호환성, 연결 유지 부담
   
3️⃣ Polling (주기적 조회): ✅ 선택!
   장점: 구현 단순, ALB 호환 완벽, Stateless
   단점: 네트워크 트래픽 증가
```

### Polling 방식 선택 이유
```
✅ 구현 단순성
   - 일반 HTTP GET 요청만 사용
   - 추가 라이브러리 불필요
   
✅ ALB 완벽 호환
   - WebSocket은 Sticky Session 필요
   - Polling은 매 요청마다 다른 서버 가능
   
✅ Stateless 아키텍처
   - 서버 메모리에 연결 상태 저장 안 함
   - Auto Scaling 시 문제 없음
   
✅ 프로젝트 규모 적합
   - 1초 간격이면 충분히 실시간처럼 느껴짐
   - 네트워크 트래픽 감당 가능
```

---

## 🔄 Polling 기반 실시간 동기화

### 클라이언트 (Frontend)

```javascript
// jtmRace.html
function startPolling() {
    setInterval(() => {
        fetch('/api/status')
            .then(response => response.json())
            .then(data => {
                // 1. 타이머 업데이트
                updateTimer(data.timer);
                
                // 2. 말 위치 업데이트
                updateHorsePositions(data.horses);
                
                // 3. 게임 상태 업데이트
                updateGameStatus(data.status);
            });
    }, 1000); // 1초마다 서버에 요청
}

동작:
├─ 1초마다 GET /api/status 호출
├─ 서버에서 현재 게임 상태 조회
├─ 화면에 반영 (타이머, 말 위치 등)
└─ 무한 반복
```

### 서버 (Backend)

```java
// RaceController.java
@GetMapping("/api/status")
public ResponseEntity<RaceResponse> getStatus() {
    // DB에서 현재 게임 상태 조회
    RaceResponse response = raceService.getRaceStatus();
    return ResponseEntity.ok(response);
}

// RaceService.java
public RaceResponse getRaceStatus() {
    // 1. race_status 테이블 조회
    Map<String, Object> status = jdbcTemplate.queryForMap(
        "SELECT * FROM race_status WHERE id = 1"
    );
    
    // 2. horses 테이블 조회
    List<Horse> horses = jdbcTemplate.query(
        "SELECT * FROM horses ORDER BY id",
        (rs, rowNum) -> {
            Horse horse = new Horse();
            horse.setName(rs.getString("name"));
            horse.setPosition(rs.getInt("position"));
            return horse;
        }
    );
    
    // 3. 응답 생성
    RaceResponse response = new RaceResponse();
    response.setStatus(status.get("status"));
    response.setTimer(status.get("timer"));
    response.setHorses(horses);
    return response;
}

특징:
├─ 단순 SELECT 쿼리 (빠름)
├─ 모든 WAS가 동일한 응답 반환
└─ Stateless (서버 메모리 사용 안 함)
```

---

## 🎯 게임 로직의 두 가지 역할

### 역할 1: 게임 진행 (Write) - Leader만 수행

```java
@Scheduled(fixedRate = 1000) // 1초마다 실행
public void updateGlobalTimer() {
    // 리더 선출 (Leader Election)
    int updatedRows = jdbcTemplate.update(
        "UPDATE race_status SET leader_ip = ? WHERE ..."
    );
    
    if (updatedRows == 0) return; // Follower는 종료
    
    // 리더만 실행
    String status = getCurrentStatus();
    
    if ("READY".equals(status)) {
        // 대기 중: 타이머 감소
        jdbcTemplate.update(
            "UPDATE race_status SET timer = timer - 1"
        );
        
        if (timer == 0) {
            startRace(); // 경주 시작
        }
    }
    else if ("RACING".equals(status)) {
        // 경주 중: 말 이동
        updateHorsePositions();
        
        if (anyHorseFinished()) {
            finishRace(); // 경주 종료
        }
    }
    else if ("FINISHED".equals(status)) {
        // 결과 표시 후 리셋
        if (timer == 0) {
            resetRace(); // 다음 경주 준비
        }
    }
}

특징:
├─ 사용자 수와 무관 (항상 동일한 연산량)
├─ Leader 1대만 수행
└─ DB에 상태 저장 (Write)
```

### 역할 2: 사용자 요청 처리 (Read) - 모든 서버 수행

```java
// 모든 WAS가 처리
@GetMapping("/api/status")
public ResponseEntity<RaceResponse> getStatus() {
    // DB에서 조회만 (Read)
    return ResponseEntity.ok(raceService.getRaceStatus());
}

@PostMapping("/api/bet")
public ResponseEntity<?> placeBet(@RequestBody BetRequest request) {
    // 베팅 처리 (Write)
    return ResponseEntity.ok(raceService.placeBet(request));
}

특징:
├─ 사용자 수에 비례 (기하급수적 증가)
├─ 모든 WAS가 나눠서 처리
└─ ALB가 부하 분산
```

---

## 📊 부하 분석

### 게임 진행 부하 (Write)

```
연산량:
├─ 타이머 감소: UPDATE 1회 (0.001초)
├─ 말 4마리 이동: UPDATE 4회 (0.004초)
├─ 승자 판정: SELECT 1회 (0.001초)
└─ 총: 0.006초/초

특징:
✅ 사용자 1명이든 100만 명이든 동일
✅ CPU 사용률: 1% 미만
✅ Leader 1대만 수행
```

### 사용자 요청 부하 (Read/Traffic)

```
사용자 1명:
├─ GET /api/status: 1회/초
├─ POST /api/bet: 0.1회/초 (가끔)
└─ 총: 1.1 RPS

사용자 100명:
├─ GET /api/status: 100회/초
├─ POST /api/bet: 10회/초
└─ 총: 110 RPS

사용자 500명:
├─ GET /api/status: 500회/초
├─ POST /api/bet: 50회/초
└─ 총: 550 RPS

특징:
❗ 사용자 수에 비례하여 기하급수적 증가
❗ CPU 사용률: 사용자 수에 따라 증가
✅ 모든 WAS가 나눠서 처리
```

---

## 🔄 전체 데이터 흐름

### 정상 운영 시 (WAS 2대)

```
[클라이언트]
사용자 A: GET /api/status (1초마다)
사용자 B: GET /api/status (1초마다)
사용자 C: POST /api/bet (베팅)
...
     ↓
[ALB] - 부하 분산
     ↓
┌─────────────┐  ┌─────────────┐
│   WAS 1     │  │   WAS 2     │
│  (Leader)   │  │ (Follower)  │
├─────────────┤  ├─────────────┤
│ 게임 로직   │  │ 게임 로직   │
│ 실행 ✅     │  │ 대기 ⏸️     │
├─────────────┤  ├─────────────┤
│ 사용자 요청 │  │ 사용자 요청 │
│ 50% 처리    │  │ 50% 처리    │
└──────┬──────┘  └──────┬──────┘
       │                │
       └────────┬────────┘
                ↓
           ┌─────────┐
           │   DB    │
           │ (MySQL) │
           └─────────┘

WAS 1 (Leader):
├─ 1초마다: 타이머 감소, 말 이동 (Write)
└─ 사용자 요청 50% 처리 (Read/Write)

WAS 2 (Follower):
├─ 1초마다: 리더 선출 시도 (실패)
└─ 사용자 요청 50% 처리 (Read/Write)
```

### Auto Scaling 시 (WAS 10대)

```
[클라이언트]
사용자 500명: 각자 1초마다 요청
     ↓
[ALB] - 10대로 분산
     ↓
┌────┐ ┌────┐ ┌────┐     ┌────┐
│WAS1│ │WAS2│ │WAS3│ ... │WAS10│
│Lead│ │Foll│ │Foll│     │Foll│
├────┤ ├────┤ ├────┤     ├────┤
│게임│ │대기│ │대기│     │대기│
│100%│ │ 0% │ │ 0% │     │ 0% │
├────┤ ├────┤ ├────┤     ├────┤
│요청│ │요청│ │요청│     │요청│
│10% │ │10% │ │10% │     │10% │
└─┬──┘ └─┬──┘ └─┬──┘     └─┬──┘
  └──────┴──────┴───────────┘
              ↓
         ┌─────────┐
         │   DB    │
         └─────────┘

효과:
✅ Leader 부담 감소 (50% → 10%)
✅ 전체 처리량 10배 증가
✅ 응답 시간 유지 (50ms)
```

---

## 💡 Polling 방식의 장단점

### 장점

```
1. 구현 단순성
   ├─ 일반 HTTP GET 요청만 사용
   ├─ 추가 라이브러리 불필요
   └─ 학습 곡선 낮음

2. ALB 완벽 호환
   ├─ 매 요청마다 다른 서버 가능
   ├─ Sticky Session 불필요
   └─ Auto Scaling 문제 없음

3. Stateless 아키텍처
   ├─ 서버 메모리에 상태 저장 안 함
   ├─ 서버 재시작 시 영향 없음
   └─ 수평 확장 용이

4. 디버깅 용이
   ├─ 일반 HTTP 요청이라 추적 쉬움
   ├─ 브라우저 개발자 도구로 확인 가능
   └─ 로그 분석 간단
```

### 단점

```
1. 네트워크 트래픽 증가
   ├─ 1초마다 요청 발생
   ├─ 사용자 500명 = 500 RPS
   └─ 데이터 전송 비용 증가

2. 서버 부하
   ├─ 변경 없어도 계속 요청
   ├─ DB 조회 부하 증가
   └─ 대규모 시 Redis 캐싱 필요

3. 실시간성 제약
   ├─ 최대 1초 지연 발생
   ├─ WebSocket보다 느림
   └─ 초정밀 타이밍 게임에는 부적합

4. 배터리 소모
   ├─ 모바일 환경에서 배터리 소모
   └─ Background에서도 계속 요청
```

### 우리 프로젝트에서는?

```
✅ 장점이 단점보다 큼

이유:
1. 게임 특성상 1초 간격이면 충분
   (경마 게임은 초정밀 타이밍 불필요)

2. 사용자 규모가 크지 않음
   (500명 이하 → 트래픽 감당 가능)

3. 구현 복잡도 최소화
   (학습 프로젝트 목적에 부합)

4. ALB + Auto Scaling 완벽 호환
   (고가용성 구현 용이)
```

---

## 🎯 트러블슈팅과의 연결

### 문제 발생 배경

```
Polling 방식 + 분산 환경:

[클라이언트]
500명이 1초마다 GET /api/status 요청
     ↓
[ALB] - 부하 분산
     ↓
┌─────────┐  ┌─────────┐
│ WAS 1   │  │ WAS 2   │
│         │  │         │
│ 게임 로직│  │ 게임 로직│
│ 실행 ❌ │  │ 실행 ❌ │  ← 둘 다 실행!
│         │  │         │
│ timer-- │  │ timer-- │  ← 동시 실행!
└────┬────┘  └────┬────┘
     │            │
     └─────┬──────┘
           ↓
      ┌─────────┐
      │   DB    │
      │ timer=28│  ← 2초 감소!
      └─────────┘

문제:
├─ 게임 로직이 각 WAS에서 독립 실행
├─ 타이머가 2배속으로 감소
└─ 사용자는 Polling으로 2배속 타이머를 봄
```

### 해결 후

```
[클라이언트]
500명이 1초마다 GET /api/status 요청
     ↓
[ALB] - 부하 분산
     ↓
┌─────────┐  ┌─────────┐
│ WAS 1   │  │ WAS 2   │
│ (Leader)│  │(Follower)│
│         │  │         │
│ 게임 로직│  │ 게임 로직│
│ 실행 ✅ │  │ 대기 ⏸️ │  ← Leader만 실행!
│         │  │         │
│ timer-- │  │  (대기) │
└────┬────┘  └────┬────┘
     │            │
     └─────┬──────┘
           ↓
      ┌─────────┐
      │   DB    │
      │ timer=29│  ← 1초만 감소 ✅
      └─────────┘

해결:
├─ Leader Election으로 1대만 실행
├─ 타이머가 정상 속도로 감소
└─ 사용자는 Polling으로 정상 타이머를 봄
```

---

## 📝 발표 시 설명 순서

### 1단계: 게임 소개 (30초)
"저희 프로젝트는 **실시간 경마 베팅 게임**입니다.
4마리의 말이 경주하고, 사용자들이 베팅하는 방식입니다."

### 2단계: Polling 방식 설명 (30초)
"실시간 동기화를 위해 **Polling 방식**을 사용했습니다.
클라이언트가 **1초마다** 서버에 현재 상태를 요청하고,
서버는 DB에서 조회한 게임 상태를 응답합니다.

WebSocket 대신 Polling을 선택한 이유는
**구현이 단순**하고 **ALB와 완벽히 호환**되기 때문입니다."

### 3단계: 게임 로직 설명 (30초)
"게임 로직은 두 가지로 나뉩니다.

첫째, **게임 진행 로직**은 1초마다 타이머를 감소시키고
말을 이동시키는 작업으로, **사용자 수와 무관**하게
항상 동일한 연산량을 가집니다.

둘째, **사용자 요청 처리**는 Polling으로 들어오는
수많은 조회 요청을 처리하는 것으로,
**사용자 수에 비례**하여 증가합니다."

### 4단계: 트러블슈팅 연결 (10초)
"문제는 WAS를 2대로 늘렸을 때 발생했습니다.
각 WAS가 독립적으로 게임 로직을 실행하면서
타이머가 **2배 속도**로 감소하는 현상이 발생했고,
이를 **Leader Election**으로 해결했습니다."

---

## 🎤 발표 스크립트 (통합 버전)

```
"저희 프로젝트는 실시간 경마 베팅 게임입니다.

실시간 동기화를 위해 Polling 방식을 사용했는데,
클라이언트가 1초마다 서버에 게임 상태를 요청하고
서버는 DB에서 조회한 데이터를 응답합니다.

WebSocket 대신 Polling을 선택한 이유는
구현이 단순하고 ALB와 완벽히 호환되며
Auto Scaling 시에도 문제가 없기 때문입니다.

게임 로직은 두 가지로 나뉩니다.
첫째, 게임 진행 로직은 타이머 감소와 말 이동으로
사용자 수와 무관하게 항상 동일한 연산량을 가집니다.
둘째, 사용자 요청 처리는 Polling으로 들어오는
조회 요청을 처리하는 것으로 사용자 수에 비례합니다.

문제는 WAS를 2대로 늘렸을 때 발생했습니다.
각 WAS가 독립적으로 게임 로직을 실행하면서
타이머가 2배 속도로 감소했고,
이를 Leader Election으로 해결했습니다."
```

---

이제 게임 구조를 먼저 설명하면 트러블슈팅이 훨씬 이해하기 쉬워질 거예요! 🎮
