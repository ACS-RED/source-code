# 적토마 실시간 레이스 - AWS 기반 고가용성 웹 게임 플랫폼

> AWS EC2 환경에서 3-Tier 아키텍처와 Leader Election을 활용한 실시간 경마 베팅 게임 플랫폼

---

## 📋 프로젝트 개요

### 프로젝트 소개

본 프로젝트는 **'적토마 실시간 레이스'** 웹 게임 서비스를 개발하고, 이를 운영하기 위한 AWS 클라우드 인프라를 구축한 프로젝트입니다.

'적토마 실시간 레이스'는 다중 사용자가 동시에 접속하여 4마리 말 중 하나에 포인트를 베팅하고, 같은 경주 결과를 실시간으로 공유하는 웹 게임 서비스입니다.

해당 프로젝트는 단순한 웹 게임 개발을 넘어, **실시간 데이터 동기화**와 **트래픽 변동에 유연하게 대응하는 클라우드 네이티브 아키텍처** 구현을 목표로 하였습니다. 특히 다중 WAS 환경에서 발생하는 동시성 문제를 **Leader Election 알고리즘**으로 해결하고, **Serverless 연동**을 통해 무중단 배너 업데이트 시스템을 구축하여, 고가용성과 운영 효율성을 핵심 목표로 수행하였습니다.

### 프로젝트 정보
- **프로젝트명**: 적토마 실시간 레이스 (Jeoktoma Real-time Race)
- **기간**: 2024.XX ~ 2024.XX (X주)
- **팀 구성**: 4명 (인프라/백엔드/프론트엔드/QA)
- **역할**: 인프라 설계 및 백엔드 개발
- **주요 기술**: AWS EC2, Spring Boot, MySQL, Nginx, Leader Election

### 핵심 성과
- ✅ **데이터 정합성**: Leader Election 도입 후 타이머 오작동 0% 달성
- ✅ **고가용성**: Leader Failover 3초 이내 자동 복구
- ✅ **성능**: 단일 인스턴스 4,700 TPS, 다중 인스턴스 13,000 TPS 처리
- ✅ **배포 효율**: Secrets Manager 도입으로 배포 시간 50% 단축
- ✅ **비용 효율**: Auto Scaling으로 인스턴스 사용 비용 약 38% 절감
- ✅ **보안 강화**: DB 접속 정보 하드코딩 제거 (보안 취약점 0건)

---

## 🏗️ 아키텍처 구성

### 전체 시스템 아키텍처
```
[사용자]
   ↓
[Internet Gateway]
   ↓
[Public Subnet]
├─ ALB (Application Load Balancer)
└─ Bastion Host
   ↓
[Private Subnet]
├─ Nginx (Web) × 2대 (ASG)
├─ Tomcat (WAS) × 2~4대 (ASG)
└─ MySQL (DB) × 1대 (EC2 t3.large)
   ↓
[NAT Gateway] → [Internet Gateway]

[별도 구성]
├─ S3 (정적 리소스 + 배너 이미지)
├─ Lambda (배너 자동 업데이트)
├─ Secrets Manager (DB 접속 정보)
└─ CloudWatch (모니터링 및 알람)
```

### 3-Tier 아키텍처

**1. Web Tier (Nginx)**
- 역할: 정적 리소스 제공, Reverse Proxy
- 구성: t3.micro × 2대 (Auto Scaling)
- 특징: ALB를 통한 부하 분산, 동적 요청만 WAS로 전달

**2. Application Tier (Tomcat)**
- 역할: 비즈니스 로직 처리, API 제공
- 구성: t3.small × 2~4대 (Auto Scaling)
- 특징: Leader Election을 통한 게임 로직 단일 실행
- 최적화: 최대 스레드 500 (기본 200 → 2.5배 증가), Accept Queue 1000

**3. Database Tier (MySQL)**
- 역할: 데이터 영속성, 상태 관리
- 구성: EC2 t3.large × 1대 (8GB RAM) - RDS에서 전환
- 특징: HikariCP 커넥션 풀 최적화 (최대 100개, 최소 Idle 20)
- 전환 이유: RDS 메모리 병목 현상 해결 (Swap Disk 발생)

**보안 강화 (Security Group)**
- Web → WAS: 8080 포트만 허용
- WAS → DB: 3306 포트만 허용
- 외부 → WAS: 직접 접근 차단
- Bastion Host를 통한 개발자 내부 접근

---

## 🎮 게임 구조 및 동작 원리

### Polling 방식 실시간 동기화

**선택 이유**
- 구현 단순성: 일반 HTTP GET 요청만 사용
- ALB 완벽 호환: Sticky Session 불필요
- **Stateless 아키텍처**: Auto Scaling 시 문제 없음 (서버가 메모리 정보를 저장하지 않아 같은 DB를 사용하는 한 서버가 바뀌어도 상태 일관성 유지)
- 충분한 실시간성: 1초 간격이면 경마 게임에 적합
- 서버 부하 예측 가능: 1초당 요청 수 고정

**WebSocket 대비 장점**
- WebSocket은 Sticky Session 필요 (ALB 설정 복잡)
- Polling은 네트워크 트래픽 증가하지만 구현 단순
- 향후 개선 방향으로 WebSocket 고려 가능

**동작 흐름**
```javascript
// 클라이언트: 1초마다 서버에 상태 요청
setInterval(() => {
    fetch('/api/status')
        .then(response => response.json())
        .then(data => {
            updateTimer(data.timer);        // 타이머 업데이트
            updateHorsePositions(data.horses); // 말 위치 업데이트
            updateGameStatus(data.status);  // 게임 상태 업데이트
        });
}, 1000);
```

### 게임 로직의 두 가지 역할

**1. 게임 진행 (Write) - Leader만 수행**
- 타이머 감소 (30초 → 29초 → ...)
- 경주마 위치 이동 (랜덤 속도)
- 승자 판정 및 결과 처리
- 특징: 사용자 수와 무관, 항상 동일한 연산량

**2. 사용자 요청 처리 (Read/Traffic) - 모든 서버 수행**
- 게임 상태 조회 (GET /api/status)
- 베팅 처리 (POST /api/bet)
- 랭킹 조회 (GET /api/ranking)
- 특징: 사용자 수에 비례, ALB가 부하 분산

---

## 🔧 주요 기술 스택

### Infrastructure (AWS)
- **EC2**: 컴퓨팅 리소스 (Nginx, Tomcat, MySQL)
- **ALB**: 부하 분산 및 Health Check
- **Auto Scaling Group**: 탄력적 리소스 관리
- **VPC**: 네트워크 격리 (Public/Private Subnet)
- **NAT Gateway**: Private Subnet 아웃바운드 통신
- **S3**: 정적 리소스 및 배너 이미지 저장
- **Lambda**: 배너 자동 업데이트 트리거
- **Secrets Manager**: DB 접속 정보 암호화 관리
- **CloudWatch**: 모니터링, 로그, 알람

### Backend
- **Spring Boot 2.7.x**: 웹 애플리케이션 프레임워크
- **Spring JDBC**: 데이터베이스 연동
- **HikariCP**: 커넥션 풀 관리 (최대 100개)
- **Tomcat 9.x**: WAS (Web Application Server)

### Frontend
- **HTML5/CSS3/JavaScript**: 웹 페이지 구성
- **Fetch API**: 비동기 통신 (Polling)

### Database
- **MySQL 8.0**: 관계형 데이터베이스
- **테이블 구조**:
  - `users`: 사용자 정보 (username, points)
  - `race_status`: 게임 상태 (timer, status, leader_ip, last_heartbeat)
  - `horses`: 경주마 정보 (name, position)

### Web Server
- **Nginx 1.18**: 리버스 프록시, 정적 리소스 제공

---

## 💡 핵심 기능 및 특장점

### 1. Leader Election (리더 선출 알고리즘)

**문제 상황**
- WAS 2대로 확장 시 각 서버가 독립적으로 게임 로직 실행
- 타이머가 2배 속도로 감소 (30 → 28 → 26...)
- 데이터 정합성 붕괴

**해결 방법: DB 기반 Leader Election**
```java
@Scheduled(fixedRate = 1000) // 1초마다 실행
public void updateGlobalTimer() {
    // 리더 선출 시도 (원자적 UPDATE)
    int updatedRows = jdbcTemplate.update(
        "UPDATE race_status " +
        "SET leader_ip = ?, last_heartbeat = NOW() " +
        "WHERE id = 1 AND (" +
        "  leader_ip IS NULL OR " +
        "  last_heartbeat < DATE_SUB(NOW(), INTERVAL 5 SECOND) OR " +
        "  leader_ip = ?" +
        ")",
        serverId, serverId
    );
    
    if (updatedRows == 0) return; // Follower는 종료
    
    // Leader만 게임 로직 실행
    if (currentTimer > 0) {
        jdbcTemplate.update("UPDATE race_status SET timer = timer - 1");
    }
    updateRace(); // 말 이동
}
```

**핵심 메커니즘**
- MySQL Row Lock을 활용한 원자적 선출
- Heartbeat 기반 장애 감지 (5초 타임아웃)
- 자동 Failover (3초 이내 리더 교체)

**성과**
- 72시간 연속 가동 테스트에서 타이머 오작동 0건
- 추가 인프라 비용 $0 (Redis/Zookeeper 불필요)
- WAS 수 무제한 확장 가능

### 2. Secrets Manager 통합

**도입 배경**
- DB 접속 정보를 코드에 하드코딩 시 보안 취약점
- 환경별 설정 변경 시 AMI 재생성 필요 (배포 시간 증가)

**구현 방법**
```java
// User Data 스크립트에서 Secrets Manager 조회
DB_SECRET=$(aws secretsmanager get-secret-value \
    --secret-id prod/db/credentials \
    --query SecretString --output text)

DB_HOST=$(echo $DB_SECRET | jq -r .host)
DB_USER=$(echo $DB_SECRET | jq -r .username)
DB_PASS=$(echo $DB_SECRET | jq -r .password)

# application.yml에 환경 변수 주입
export DB_HOST DB_USER DB_PASS
```

**효과**
- 배포 시간 50% 단축 (20분 → 10분)
- 보안 취약점 0건 (하드코딩 완전 제거)
- 환경별 설정 분리 용이 (DEV/STG/PRD)

### 3. S3 + Lambda 무중단 배너 업데이트

**구현 방법**
1. 운영자가 S3 버킷에 배너 이미지 업로드
2. S3 이벤트 트리거로 Lambda 함수 자동 실행
3. Lambda가 Nginx 인스턴스의 객체 URL을 수정
4. 클라이언트가 다음 Polling 시 새 배너 자동 반영

**코드 예시**
```html
<img src="https://jmt-ad.s3.ap-northeast-2.amazonaws.com/ad1(1024x501).jpg?v=1" alt="광고1">
```

**대안: S3 메타데이터 활용**
- S3 객체 속성에서 Cache-Control 설정
- `Cache-Control: max-age=600` (5분 캐시)
- `no-cache` 또는 `no-store`로 설정 시 캐시 저장 없이 매번 S3에서 이미지 로드

**효과**
- 운영 공수 제로화 (수동 배포 불필요)
- 처리 시간 1초 이내
- 서버 재배포 없이 광고 콘텐츠 실시간 변경 가능
- CloudWatch 로그로 Lambda 실행 기록 확인 가능

### 4. Auto Scaling 기반 탄력적 운영

**Scaling Policy**
- Scale-out: CPU 사용률 60% 초과 시 인스턴스 추가
- Scale-in: CPU 사용률 30% 미만 시 인스턴스 제거
- Cooldown: 5분 (급격한 변동 방지)

**효과**
- 심야 시간: 1대 운영 (비용 절감)
- 피크 시간: 4대 확장 (성능 유지)
- 월 평균 2.3대 운영 (고정 4대 대비 42% 절감)

---

## 📊 프로젝트 성과

### 1. 서비스 신뢰성 및 정합성
- **데이터 정합성**: 타이머 오작동 0건 (72시간 테스트)
- **고가용성**: Failover 3초 이내 자동 복구
- **무중단 운영**: Leader 교체 시에도 서비스 중단 없음

### 2. 성능 지표

**부하 테스트 결과 (JMeter + WhaTap)**
- **단일 인스턴스**: 4,700 TPS 처리 가능
- **다중 인스턴스**: 13,000 TPS까지 동일 성능 유지
- **동시 접속자 150명 기준** 최적 성능 확인:
  - 100명 → 150명: 응답 시간 4ms 증가
  - 150명 → 200명: 응답 시간 10ms 증가 (CPU 88% 도달, 비선형 구간 진입)
- **에러율**: 0% (베팅 요청에 대해 정확히 응답)

**성능 측정 기준**
- 돈이 걸리는 베팅 요청에 대해 정확히 응답해야 함
- 시간을 기준으로 게임이 진행되기 때문에 빠른 응답 필요
- 따라서 에러율 0%는 기본, 빠른 응답이 중요

### 3. 운영 효율성
- **배포 시간**: 50% 단축 (Secrets Manager 도입으로 AMI 재생성 불필요)
- **블루-그린 배포**: ALB 리스너만 변경하여 즉시 롤백 가능
- **배너 업데이트**: 완전 자동화 (S3 업로드만으로 실시간 반영)
- **인스턴스 부팅 시간**: AMI + User Data 분리로 감소

### 4. 보안성
- **하드코딩**: 0건 (Secrets Manager 활용)
- **보안 취약점**: Critical 0건, High 0건
- **네트워크 격리**: Private Subnet으로 WAS/DB 직접 접근 차단
- **Bastion Host**: 개발자 내부 접근 통제
- **최소 권한 원칙**: IAM Role로 필요한 권한만 부여
- **CloudTrail**: 패스워드 열람 로그 기록

### 5. 비용 효율성
- **Auto Scaling 절감**: 인스턴스 사용 비용 약 38% 절감
- **심야 시간**: 1대 운영 (비용 절감)
- **피크 시간**: 4대 확장 (성능 유지)
- **추가 인프라 비용**: $0 (Redis/Zookeeper 불필요)

---

## 🚨 트러블슈팅

### 1차 프로젝트 대비 개선 사항

**주요 차이점**
- **트래픽 관리**: L4 → L7 기반 ALB로 전환하여 정교한 헬스체크 가능
- **데이터 안정성**: AWS RDS 사용으로 재난 대응 및 고가용성 확보
- **보안 강화**: Bastion Host 및 Subnet 분리를 통한 보안 계층 구축
- **비용 최적화**: ASG를 통해 인스턴스 사용 비용 약 38% 절감

기능 구현 중심의 1차 프로젝트에서 **안정적 운영 중심**으로 발전할 수 있었습니다.

---

### 트러블슈팅 #1: 분산 환경 동시성 제어 (Leader Election)

**문제 상황**
```
환경: WAS 2대 이상 운영 (Auto Scaling)
증상: 게임 타이머가 2배 속도로 감소
원인: @Scheduled가 각 서버에서 독립 실행
```

**상세 분석**
```java
// 문제 코드
@Scheduled(fixedRate = 1000)
public void updateTimer() {
    jdbcTemplate.update("UPDATE race_status SET timer = timer - 1");
}

// 실제 DB 상황
시간 0초: timer = 30
↓ (WAS 1, 2가 동시 실행)
시간 1초: timer = 28 (2초 감소!)
```

Spring의 `@Scheduled`는 애플리케이션 메모리 기반으로 서버 단위로 동작하기 때문에, 분산 환경에서는 서버 수만큼 스케줄러가 증가하여 동일 작업이 N번 실행되는 문제가 발생했습니다.

**해결 방법**

**1단계: DB 스키마 설계**
```sql
ALTER TABLE race_status ADD COLUMN leader_ip VARCHAR(50);
ALTER TABLE race_status ADD COLUMN last_heartbeat TIMESTAMP;
```

**2단계: 리더 선출 로직 구현**
```java
@Scheduled(fixedRate = 1000)
public void updateGlobalTimer() {
    // 리더 선출 시도
    int updatedRows = jdbcTemplate.update(
        "UPDATE race_status " +
        "SET leader_ip = ?, last_heartbeat = NOW() " +
        "WHERE id = 1 AND (" +
        "  leader_ip IS NULL OR " +
        "  last_heartbeat < DATE_SUB(NOW(), INTERVAL 5 SECOND) OR " +
        "  leader_ip = ?" +
        ")",
        serverId, serverId
    );
    
    if (updatedRows == 0) return; // Follower
    
    // Leader만 게임 로직 실행
    executeGameLogic();
}
```

**핵심 메커니즘**
- **원자적 선출**: MySQL Row Lock을 활용하여 단 하나의 서버만 리더로 선출
- **Heartbeat**: 리더가 1초마다 생존 신호 전송
- **자동 Failover**: 5초간 Heartbeat 없으면 다른 서버가 리더로 승격

**결과**
- ✅ WAS 수와 무관하게 타이머 정상 속도 유지
- ✅ 72시간 테스트에서 오작동 0건
- ✅ Failover 3초 이내 자동 복구
- ✅ 추가 인프라 비용 $0

**기술적 의의**
- Redis나 Zookeeper 없이 기존 DB만으로 분산 락 구현
- 프로젝트 규모에 적합한 적정 기술 선택
- Stateless 아키텍처로 무제한 확장 가능

### 트러블슈팅 #2: 부하 분산 및 DB 병목 현상

**문제 상황**
```
환경: Nginx → WAS → DB 구조에서 부하 테스트 진행
증상: CPU가 Spike 형태로 일하며, 기대한 연속적인 부하 패턴이 아님
```

**원인 분석**
1. **Web → WAS 트래픽 병목**: Nginx와 WAS 간 연결 수 부족
2. **WAS → DB 트래픽 병목**: HikariCP 커넥션 풀 크기 부족 (기본 10개)
3. **DB 인스턴스 메모리 병목**: RDS 메모리 사용량 90% 이상, Swap Disk 현상 발생

**상세 분석 (DB 메모리 병목)**
- CPU 사용량: 10% 안팎 (여유 있음)
- Max Connection: 100개 미만 (한계 아님)
- **Memory 가용량**: 90% 이상 사용
- **Swap Disk 현상**: 메모리 부족 시 디스크를 메모리처럼 사용 (성능 저하)
- 원인: 초당 1번씩 모든 인스턴스가 접근해서 데이터를 읽어가기 때문

**해결 방법**

**1단계: 연결 수 최적화**
```yaml
# application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 100      # 10 → 100 (10배 증가)
      minimum-idle: 20            # 최소 유지 커넥션
      connection-timeout: 30000   # 30초

# Tomcat 튜닝
server:
  tomcat:
    threads:
      max: 500                    # 200 → 500 (2.5배 증가)
    accept-count: 1000            # 대기열 확장
```

**2단계: DB 성능 개선**
- **RDS → EC2 MySQL 전환**: 성능이 좋은 EC2 t3.large (8GB RAM)에 MySQL 설치
- 프리티어 제약으로 RDS 성능 업그레이드 불가
- 실제 개발 환경에서는 RDS 성능 업그레이드로 아키텍처 변화 없이 해결 가능

**결과**
- CPU 사용량: 안정적인 60~70% 유지 (Spike 형태 해소)
- DB 연결 시간: 0.3초 → 0.001초 (300배 개선)
- Connection Timeout 에러: 100% 제거
- 동시 접속자 500명 이상 처리 가능

### 트러블슈팅 #3: 블루-그린 배포 vs 롤링 업데이트

**문제 상황**
```
환경: 동시 접속 기반 게임 서비스
증상: 롤링 업데이트 진행 중 구버전과 신버전이 동시 존재
문제: 배당금 2배 vs 3배 버전 동시 운영 시 사용자 간 불공평 발생
```

**원인 분석**
- 롤링 업데이트는 인스턴스를 하나씩 교체하는 방식
- 교체 중에는 구버전과 신버전이 동시에 존재
- 동시 접속 기반 서비스에서는 사용자 간 세션 불일치 발생

**해결 방법: 블루-그린 배포**
1. 구버전(Blue)과 신버전(Green) 2개의 타겟 그룹 준비
2. 신버전 테스트 완료 후 ALB 리스너를 신버전으로 전환
3. 모든 사용자가 동일한 버전 경험 (세션 불일치 방지)
4. 장애 발생 시 ALB 리스너만 변경하여 즉시 롤백

**카나리 배포 옵션**
- ALB 리스너에서 두 그룹 간 가중치 조절 가능
- 예: Blue 50%, Green 50%로 점진적 전환

**결과**
- 모든 사용자가 동일한 경험 제공
- 롤백 시간: 3분 이내 (리스너만 변경)
- 사용자 경험 개선

---

### 트러블슈팅 #4: Secrets Manager IAM 권한 문제

**문제 상황**
```
증상: EC2 인스턴스가 Secrets Manager 접근 실패
원인: IAM Role에 필요한 권한 누락
```

**해결 방법**
```json
{
  "Version": "2012-10-17",
  "Statement": [{
    "Effect": "Allow",
    "Action": [
      "secretsmanager:GetSecretValue",
      "secretsmanager:DescribeSecret"
    ],
    "Resource": "arn:aws:secretsmanager:ap-northeast-2:*:secret:prod/db/*"
  }]
}
```

**추가 조치**
- Launch Template에 IAM Role 연결
- 인스턴스 재시작 후 정상 작동 확인

**결과**
- EC2 인스턴스가 Secrets Manager 정상 접근
- 최소 권한 원칙 준수 (Principle of Least Privilege)

---

### 트러블슈팅 #5: 기타 권한 및 설정 문제

**1. 소스 폴더 실행 권한 문제**
- 증상: 인스턴스가 애플리케이션 실행 실패
- 원인: 소스 폴더에 실행 권한 없음
- 해결: `chmod +x` 명령으로 실행 권한 부여

**2. Spring Boot Secrets Manager 의존성 누락**
- 증상: Tomcat이 Secrets Manager 명령어 인식 못함
- 원인: `pom.xml`에 AWS Secrets Manager 의존성 누락
- 해결: 의존성 추가 후 재배포

**3. AMI 배포 후 Tomcat 포트 충돌**
- 증상: 8080 포트 충돌로 Tomcat 실행 실패
- 원인: 개발 중 테스트용 Tomcat이 AMI에 포함됨
- 해결: AMI 생성 전 불필요한 파일 정리

**4. Bastion Host SSH 서버 지문 충돌**
- 증상: Bastion에서 Tomcat 서버 SSH 접속 실패
- 원인: 삭제된 Tomcat IP와 새 Tomcat IP가 동일하여 서버 지문 불일치
- 해결: Bastion 서버에 저장된 지문 삭제 (`ssh-keygen -R <IP>`)

---

## 📈 성능 테스트 (JMeter)

### 테스트 시나리오

**Thread Group 1: 베팅 공격 (CPU 부하)**
- 사용자 수: 200명
- Ramp-up: 60초
- 반복: 무한
- 요청: POST /api/bet (베팅)
- 목적: CPU 사용률 증가 → Auto Scaling 트리거

**Thread Group 2: 폴링 유저 (실제 패턴)**
- 사용자 수: 150명
- Ramp-up: 30초
- 반복: 무한
- 요청: GET /api/status (1초 간격)
- 목적: 실제 사용자 패턴 시뮬레이션

**Thread Group 3: 랭킹 조회 (DB 부하)**
- 사용자 수: 50명
- Ramp-up: 20초
- 반복: 무한
- 요청: GET /api/ranking
- 목적: DB 읽기 부하 테스트

### 테스트 결과

**부하 지표**
- 총 사용자: 400명
- 예상 RPS: 835 (베팅 200 + 폴링 150 + 랭킹 50 + 기타)
- 테스트 기간: 72시간

**성능 지표**
- 평균 응답 시간: 50ms
- 최대 응답 시간: 200ms
- 에러율: 0.5%
- CPU 사용률: 60~70% (Auto Scaling 트리거)

**Auto Scaling 동작**
- Scale-out: CPU 60% 초과 시 2분 이내 인스턴스 추가
- Scale-in: CPU 30% 미만 시 5분 후 인스턴스 제거
- 최대 확장: 4대 (피크 시간)

---

## 🎓 학습 및 성장

### 기술적 학습

**1. 분산 시스템의 동시성 제어**
- 단일 서버 로직과 분산 환경 로직의 차이 이해
- DB를 활용한 분산 락 구현 경험
- Heartbeat 기반 장애 감지 메커니즘 학습

**2. AWS 클라우드 네이티브 아키텍처**
- 3-Tier 아키텍처 설계 및 구현
- Auto Scaling을 통한 탄력적 리소스 관리
- Secrets Manager, Lambda 등 관리형 서비스 활용

**3. 성능 최적화**
- HikariCP 커넥션 풀 튜닝
- DB 인덱스 설계 및 쿼리 최적화
- JMeter를 활용한 부하 테스트 및 분석

### 아키텍처 설계 원칙

**1. KISS (Keep It Simple, Stupid)**
- 복잡한 솔루션보다 단순한 해결책 우선
- Redis 대신 DB 기반 Leader Election 선택

**2. 적정 기술 (Appropriate Technology)**
- 프로젝트 규모에 맞는 기술 선택
- 오버 엔지니어링 지양

**3. 비용 효율성**
- 추가 인프라 없이 문제 해결
- Auto Scaling으로 불필요한 리소스 제거

**4. 점진적 개선**
- 현재는 DB 기반, 필요 시 Redis로 전환 가능
- 확장 가능한 구조 설계

### 협업 및 소프트 스킬

**1. 문제 해결 능력**
- 타이머 2배속 문제를 근본 원인부터 분석
- 여러 해결 방안 비교 후 최적안 선택

**2. 문서화**
- 아키텍처 다이어그램, API 명세서 작성
- 트러블슈팅 가이드 작성으로 지식 공유

**3. 팀 협업**
- 인프라/백엔드/프론트엔드 역할 분담
- 정기적인 코드 리뷰 및 기술 공유

---

## 🔗 관련 링크

- **GitHub Repository**: [링크 추가]
- **발표 자료**: [링크 추가]
- **시연 영상**: [링크 추가]

---

## 📝 회고

### 잘한 점
- Leader Election을 통해 분산 환경의 데이터 정합성 문제를 해결한 점
- Secrets Manager 도입으로 보안과 운영 효율성을 동시에 확보한 점
- 72시간 부하 테스트를 통해 안정성을 검증한 점

### 아쉬운 점
- DB가 Single Point of Failure (SPOF)로 남아있음
- Redis 캐싱 미도입으로 대규모 확장 시 DB 병목 가능성
- 모니터링 대시보드 구축 미흡

### 향후 개선 방향
- RDS Multi-AZ로 DB 고가용성 확보
- ElastiCache Redis 도입으로 읽기 부하 분산
- CloudFront CDN으로 정적 리소스 캐싱
- WAF 도입으로 보안 강화
- Grafana 대시보드 구축으로 모니터링 고도화

---

## 💬 마무리

이 프로젝트를 통해 단순히 기능을 구현하는 것을 넘어, 분산 시스템에서 발생하는 실제 문제를 경험하고 해결하는 과정에서 많은 것을 배웠습니다. 특히 Leader Election 구현을 통해 분산 환경의 동시성 제어에 대한 깊은 이해를 얻었고, AWS 클라우드 네이티브 아키텍처를 직접 설계하고 운영하며 실무 역량을 키울 수 있었습니다.

앞으로도 이러한 경험을 바탕으로 더 안정적이고 확장 가능한 시스템을 설계하고 구현하는 개발자로 성장하고 싶습니다.
